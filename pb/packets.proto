syntax = "proto3";
package pb;

message RequestSendEmail {
    repeated string to = 1;
    string from = 2;
    string body = 3;
}

message ResponseSendEmail {
    string result = 1;
    string error = 2;
}